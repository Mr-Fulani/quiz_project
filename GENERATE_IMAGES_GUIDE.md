# Руководство по генерации изображений для задач

## Быстрый старт

### Локальная разработка

```bash
# Тестовый режим: сначала 10 задач для проверки, затем остальные группами по 100
# (батчами = группами, пакетами определенного размера)
docker compose run --rm quiz_backend python manage.py generate_all_images --batch-test

# С проверкой URL (найдет нерабочие старые ссылки из S3)
docker compose run --rm quiz_backend python manage.py generate_all_images --check-urls

# Комбинация: тест + проверка URL + пауза 0.3 сек
docker compose run --rm quiz_backend python manage.py generate_all_images --batch-test --check-urls --pause 0.3
```

### Продакшен (контейнеры уже запущены)

**⚠️ ВАЖНО:** На проде используйте `docker compose exec` вместо `docker compose run`, так как контейнеры уже запущены.

```bash
# Тестовый режим: сначала 10 задач для проверки, затем остальные группами по 100
# (батчами = группами, пакетами определенного размера)
docker compose exec quiz_backend python manage.py generate_all_images --batch-test

# С проверкой URL (найдет нерабочие старые ссылки из S3)
docker compose exec quiz_backend python manage.py generate_all_images --check-urls --pause 0.5

# Комбинация: тест + проверка URL + пауза 0.3 сек
docker compose exec quiz_backend python manage.py generate_all_images --batch-test --check-urls --pause 0.3

# Обычная обработка всех задач с проверкой URL
docker compose exec quiz_backend python manage.py generate_all_images --check-urls --pause 0.5
```

**Альтернатива (если используется старый формат docker-compose):**
```bash
docker-compose exec quiz_backend python manage.py generate_all_images --check-urls --pause 0.5
```

**Или если известен container_name:**
```bash
docker exec quiz_backend python manage.py generate_all_images --check-urls --pause 0.5
```

## Параметры команды

### Основные параметры

- `--batch-test` - Сначала обработать 10 задач для тестирования, затем запросить подтверждение и продолжить группами (батчами) по 100. **Батч** = группа задач, обрабатываемых вместе (например, 100 задач = 1 батч)
- `--check-urls` - Проверять работоспособность существующих URL перед пропуском (найдет нерабочие старые ссылки из S3)
- `--force` - Регенерировать изображения даже если они уже существуют
- `--pause 0.5` - Пауза между генерациями в секундах (по умолчанию: 0.5)
- `--batch-size 100` - Размер батча (группы) для обработки (по умолчанию: 100 задач в одной группе, используется только с --batch-test)
- `--dry-run` - Проверка без реальной генерации
- `--limit N` - Обработать только N задач (для тестирования)
- `--task-ids 1,2,3` - Обработать конкретные задачи по ID
- `--translation-group-ids uuid1,uuid2` - Обработать задачи по translation_group_id

## Примеры использования

### 1. Тестовый режим (рекомендуется для первого запуска)

```bash
# Локально
docker compose run --rm quiz_backend python manage.py generate_all_images --batch-test

# На проде
docker compose exec quiz_backend python manage.py generate_all_images --batch-test
```

**Что происходит:**
1. Обрабатываются первые 10 задач (тестовый батч)
2. Показывается статистика (успешно/ошибок)
3. Запрашивается подтверждение на продолжение
4. Если подтверждено, обрабатываются оставшиеся задачи группами (батчами) по 100:
   - Батч 1: задачи 11-110
   - Батч 2: задачи 111-210
   - Батч 3: задачи 211-310
   - и так далее...

### 2. С проверкой нерабочих URL

```bash
# Локально
docker compose run --rm quiz_backend python manage.py generate_all_images --check-urls --pause 0.5

# На проде
docker compose exec quiz_backend python manage.py generate_all_images --check-urls --pause 0.5
```

**Что происходит:**
- Проверяется доступность всех существующих URL изображений
- Если URL не работает (например, старые ссылки из S3), изображение регенерируется
- Если URL работает, задача пропускается

### 3. Принудительная регенерация всех изображений

```bash
# Локально
docker compose run --rm quiz_backend python manage.py generate_all_images --force --pause 0.5

# На проде
docker compose exec quiz_backend python manage.py generate_all_images --force --pause 0.5
```

**⚠️ Внимание:** Это регенерирует ВСЕ изображения, даже если они уже существуют и работают.

### 4. Обработка конкретных задач

```bash
# Только задачи с ID 1, 2, 3
docker compose exec quiz_backend python manage.py generate_all_images --task-ids 1,2,3

# Только задачи из определенной группы переводов
docker compose exec quiz_backend python manage.py generate_all_images --translation-group-ids uuid1,uuid2
```

### 5. Тестирование на небольшом количестве задач

```bash
# Обработать только 50 задач для тестирования
docker compose exec quiz_backend python manage.py generate_all_images --limit 50 --check-urls
```

## Рекомендации

### Для продакшена

1. **Всегда используйте `--batch-test`** для первого запуска, чтобы проверить работоспособность
2. **Используйте `--check-urls`** для поиска и регенерации нерабочих старых ссылок
3. **Установите паузу 0.3-0.5 сек** для баланса между скоростью и стабильностью
4. **Используйте `docker compose exec`** вместо `docker compose run` на проде

### Для массовой обработки (1000+ задач)

```bash
# Рекомендуемая команда для продакшена
docker compose exec quiz_backend python manage.py generate_all_images \
  --batch-test \
  --check-urls \
  --pause 0.3 \
  --batch-size 100
```

**Почему:**
- `--batch-test` - безопасный старт с тестом на 10 задачах
- `--check-urls` - найдет и исправит нерабочие старые ссылки
- `--pause 0.3` - оптимальная пауза для массовой обработки
- `--batch-size 100` - обработка батчами для контроля прогресса

## Устранение проблем

### Ошибка: "контейнер не перезапускается"

**Проблема:** На проде контейнеры уже запущены, и `docker compose run` пытается создать новый контейнер.

**Решение:** Используйте `docker compose exec` вместо `docker compose run`:

```bash
# ❌ Неправильно (на проде)
docker compose run --rm quiz_backend python manage.py generate_all_images

# ✅ Правильно (на проде)
docker compose exec quiz_backend python manage.py generate_all_images
```

### Ошибка: "Container is not running"

**Проблема:** Контейнер не запущен.

**Решение:** Запустите контейнеры:

```bash
docker compose up -d
```

### Ошибка: "Connection refused" при проверке URL

**Проблема:** URL недоступен или таймаут слишком короткий.

**Решение:** Это нормально для нерабочих старых ссылок. Скрипт автоматически регенерирует такие изображения.

## Мониторинг

### Просмотр логов

```bash
# Логи контейнера в реальном времени
docker compose logs -f quiz_backend

# Последние 100 строк логов
docker compose logs --tail=100 quiz_backend
```

### Проверка прогресса

Скрипт выводит прогресс в формате:
```
🎨 [5/100] Генерация изображения для задачи 123...
✅ [5/100] Задача 123: изображение загружено в S3
```

## После генерации

После успешной генерации изображений:

1. Проверьте статистику в конце выполнения
2. Опубликуйте задачи через Django Admin (изображения уже готовы)

