# Исправление неподдерживаемых языков задач

## Проблема

В базе данных есть задачи с переводами на языки, которые не поддерживаются сайтом Django (только `ru` и `en`). Это приводит к тому, что ссылки в Telegram опросах ведут на 404 страницы для языков типа `tr`, `ar`, etc.

## Решение

### 1. Профилактика (уже реализовано)

В `telegram_service.py` добавлена проверка языков при генерации URL для ссылок на задачи. Для неподдерживаемых языков автоматически используется английский.

### 2. Исправление существующих данных

Для исправления уже существующих задач в продакшене создана management команда Django.

## Использование

### В Docker контейнере (продакшен)

Для запуска в Docker контейнере с базой данных:

#### Предварительный анализ
```bash
# Войти в контейнер приложения
docker exec -it quiz_backend bash

# Перейти в директорию проекта
cd /app

# Запустить анализ
python manage.py fix_unsupported_languages --dry-run
```

#### Исправление данных
```bash
# Войти в контейнер приложения
docker exec -it quiz_backend bash

# Перейти в директорию проекта
cd /app

# Запустить исправление с подтверждением
python manage.py fix_unsupported_languages --fix
```

### Локально (разработка)

#### Предварительный анализ
```bash
cd /path/to/quiz_backend
python manage.py fix_unsupported_languages --dry-run
```

#### Исправление данных
```bash
cd /path/to/quiz_backend
python manage.py fix_unsupported_languages --fix
```

### Команды покажут:
- Общее количество переводов задач
- Статистику по языкам (✅ поддерживаемые, ❌ неподдерживаемые)
- Количество переводов, которые нужно исправить
- Запрос подтверждения перед реальными изменениями
- Отчет о выполненных изменениях

## Примеры вывода

### Анализ (dry-run)
```
🔍 Анализ задач с неподдерживаемыми языками

==================================================
Поддерживаемые языки сайта: ['ru', 'en']

Всего переводов задач: 15420

Статистика по языкам:
  ✅ en: 5120 переводов
  ✅ ru: 5120 переводов
  ❌ tr: 2560 переводов
  ❌ ar: 2560 переводов
  ❌ fr: 1020 переводов

⚠️  Найдено 6140 переводов с неподдерживаемыми языками:
  - tr: 2560 переводов
  - ar: 2560 переводов
  - fr: 1020 переводов

==================================================
ПРЕДВАРИТЕЛЬНЫЙ ПРОСМОТР исправлений:
Будет исправлено 6140 переводов
  tr → en: 2560 переводов
  ar → en: 2560 переводов
  fr → en: 1020 переводов

Это предварительный просмотр. Реальные изменения не внесены.

💡 Для выполнения исправлений запустите команду с флагом --fix
   python manage.py fix_unsupported_languages --fix
```

### Исправление с подтверждением
```
🔍 Анализ задач с неподдерживаемыми языками
... [анализ]

⚠️  ВНИМАНИЕ: Будет изменено 6140 переводов задач.
Это действие нельзя отменить. Продолжить? (yes/no): yes

==================================================
ИСПОЛНЕНИЕ исправлений:
Будет исправлено 6140 переводов
  tr → en: 2560 переводов
  ar → en: 2560 переводов
  fr → en: 1020 переводов

✅ Исправлено 6140 переводов задач
Все неподдерживаемые языки изменены на 'en'

==================================================
🎉 Исправление языков завершено!

Рекомендации:
- Проверьте работу сайта с английскими URL
- Убедитесь, что английские переводы корректны
- При необходимости пересоздайте кэш
```

## Файлы

- `run_fix_languages_docker.sh` - Удобный скрипт для запуска в Docker
- `fix_unsupported_languages.py` - Standalone скрипт (резервный)
- `quiz_backend/tasks/management/commands/fix_unsupported_languages.py` - Management команда Django
- `quiz_backend/tasks/services/telegram_service.py` - Исправлена генерация URL (строки 893-901)
- `FIX_UNSUPPORTED_LANGUAGES_README.md` - Эта документация

## Работа с Docker

### Важные замечания для продакшена

1. **Резервное копирование БД**
   ```bash
   # Скрипт автоматически создаст бэкап перед --fix
   # Или вручную:
   docker exec -it <db_container> pg_dump -U postgres quiz_db > backup_$(date +%Y%m%d_%H%M%S).sql
   ```

2. **Проверка контейнеров**
   ```bash
   # Убедитесь что все контейнеры работают
   docker ps

   # Проверьте логи если что-то не работает
   docker logs quiz_backend
   docker logs quiz_db
   ```

3. **Удобный скрипт запуска** ⭐
   ```bash
   # Скрипт автоматически определяет имена контейнеров
   ./run_fix_languages_docker.sh --dry-run  # анализ
   ./run_fix_languages_docker.sh --fix      # исправление с автоматическим бэкапом
   ```

   **Особенности скрипта:**
   - Автоматически находит контейнеры (работает с любыми именами)
   - Создает резервную копию перед изменениями
   - Показывает подробную информацию о найденных контейнерах
   - Работает как в интерактивном, так и автоматическом режиме

4. **Ручной запуск в фоне** (для больших объемов данных)
   ```bash
   # Запуск без интерактивного ввода
   docker exec -i quiz_backend bash -c "cd /app && echo 'yes' | python manage.py fix_unsupported_languages --fix"
   ```

4. **Мониторинг ресурсов**
   - Следите за использованием CPU и памяти во время работы скрипта
   - Для больших объемов данных операция может занять время

### Переменные окружения

Убедитесь что в контейнере доступны все необходимые переменные окружения:
- `DATABASE_URL` или параметры подключения к БД
- `DJANGO_SETTINGS_MODULE`
- `SECRET_KEY`

## Безопасность

- ✅ **Dry-run режим** по умолчанию - только анализ без изменений
- ✅ **Подтверждение** перед реальными изменениями
- ✅ **Детальная статистика** - показывает сколько и каких переводов будет исправлено
- ✅ **Логирование** всех действий
- ✅ **Работает в изолированном Docker контейнере** - изменения не влияют на хост систему

## После исправления

1. **Проверьте работу сайта** - убедитесь, что английские URL работают корректно
2. **Пересоздайте кэш** - если используется кэширование переводов
3. **Проверьте Telegram опросы** - новые опросы должны генерировать корректные ссылки
4. **Мониторьте логи** - следите за предупреждениями о замене языков