# Интеграция с социальными сетями

## Обзор

Система автоматически публикует задачи в социальные сети при установке `published=True`. Поддерживается два метода публикации:

1. **Прямая интеграция через API** (Pinterest, Яндекс Дзен, Facebook) - надежнее и быстрее
2. **Webhook интеграция** (Instagram, TikTok, YouTube Shorts) - через Make.com

---

## Архитектура

```
Задача создана/обновлена → published=True
              ↓
      Django Signal (auto_publish_to_social_media)
              ↓
   social_media_service.publish_to_social_media()
              ↓
    ┌────────────────────────────┬──────────────────────────────┐
    │   API Platforms            │   Webhook Platforms          │
    │   (Pinterest, Дзен, FB)    │   (Instagram, TikTok, YT)    │
    └────────────────────────────┴──────────────────────────────┘
              ↓                              ↓
      Прямой API запрос              Make.com Webhook
              ↓                              ↓
     Результат в БД                Callback в БД (опционально)
```

---

## Настройка API платформ

### 1. Pinterest

1. Создайте приложение: https://developers.pinterest.com/apps/
2. Получите Access Token с правами: `boards:read`, `pins:write`
3. Создайте доску (Board) в Pinterest и получите её ID
4. В Django админке → **Webhooks → Social Media Credentials** → Добавить:
   - Platform: Pinterest
   - Access Token: ваш токен
   - Extra Data: `{"board_id": "your-board-id"}`
   - Is Active: ✅

### 2. Яндекс Дзен

1. Зарегистрируйтесь в Яндекс Дзен: https://zen.yandex.ru/
2. Создайте канал
3. Получите OAuth токен через Яндекс.OAuth
4. В Django админке → **Webhooks → Social Media Credentials** → Добавить:
   - Platform: Яндекс Дзен
   - Access Token: ваш OAuth токен
   - Extra Data: `{"channel_id": "your-channel-id"}`
   - Is Active: ✅

### 3. Facebook

1. Создайте Facebook страницу
2. Создайте приложение: https://developers.facebook.com/apps/
3. Получите Page Access Token с правами: `pages_manage_posts`, `pages_read_engagement`
4. В Django админке → **Webhooks → Social Media Credentials** → Добавить:
   - Platform: Facebook
   - Access Token: ваш Page Access Token
   - Extra Data: `{"page_id": "your-page-id"}`
   - Is Active: ✅

---

## Настройка Webhook платформ (Make.com)

### Instagram, TikTok, YouTube Shorts

Эти платформы требуют сложной OAuth авторизации или одобрения разработчика. Рекомендуется использовать Make.com:

1. **Создайте сценарий в Make.com:**
   - Webhook Trigger → получает данные от Django
   - Router → разделяет по платформам
   - Модули для каждой платформы:
     - Instagram: "Create Media" (требует Business аккаунт)
     - TikTok: TikTok модуль (если доступен)
     - YouTube: YouTube Shorts API

2. **Настройте webhook в Django:**
   - Django админка → **Webhooks → Webhooks** → Добавить:
   - Service Name: "Make.com Social Media"
   - URL: ваш Make.com webhook URL
   - Webhook Type: "Социальные сети"
   - Target Platforms: `["instagram", "tiktok", "youtube_shorts"]`
   - Is Active: ✅

3. **(Опционально) Настройте callback в Make.com:**
   - Добавьте HTTP модуль в конце сценария
   - POST в: `https://your-domain.com/api/webhooks/social-media-callback/`
   - Body:
     ```json
     {
       "task_id": "{{task_id}}",
       "platform": "{{platform}}",
       "status": "published",
       "post_url": "{{post_url}}",
       "post_id": "{{post_id}}"
     }
     ```

---

## Формат данных webhook

Django отправляет в Make.com следующую структуру:

```json
{
  "task_id": 123,
  "platform": "instagram",
  "content": {
    "image_url": "https://s3.amazonaws.com/.../task.png",
    "title": "Python Quiz: What will be the output?",
    "description": "Python Quiz...\n\nВарианты: Option A, Option B...",
    "explanation": "The correct answer is B because...",
    "link": "https://your-site.com/task/123",
    "hashtags": ["#programming", "#coding", "#quiz"],
    "topic": "Python",
    "difficulty": "medium"
  },
  "metadata": {
    "language": "en",
    "translation_group_id": "uuid-here",
    "publish_date": "2025-11-30T12:00:00Z"
  }
}
```

---

## Использование

### Автоматическая публикация

При создании или обновлении задачи с `published=True`, она автоматически публикуется во все активные социальные сети.

### Ручная публикация

1. Откройте Django админку → **Tasks → Tasks**
2. Выберите задачи для публикации
3. Actions → **📱 Опубликовать в соцсети**
4. Нажмите **Go**

### Просмотр публикаций

1. Django админка → **Tasks → Social Media Posts** - все публикации
2. Или при редактировании задачи → вкладка **Social Media Posts** (inline)

---

## Мониторинг

### Статусы публикаций

- **Ожидает** (pending) - создана запись, ожидает обработки
- **Обрабатывается** (processing) - отправлено в API/webhook
- **Опубликовано** (published) - успешно опубликовано
- **Ошибка** (failed) - ошибка при публикации

### Логи

Все действия логируются в `quiz_backend_logs/debug.log`:

```bash
# Просмотр логов публикаций
docker compose exec quiz_backend tail -f /app/quiz_backend_logs/debug.log | grep "соцсети"
```

---

## Отладка

### Проблема: "Нет credentials для платформы"

**Решение:** Добавьте учетные данные в админке:
- Webhooks → Social Media Credentials → Добавить

### Проблема: "Token expired"

**Решение:** Обновите access_token в Social Media Credentials

### Проблема: "Webhook не отправляется"

**Решение:** 
1. Проверьте, что webhook активен: Webhooks → Webhooks
2. Проверьте target_platforms: должен содержать нужную платформу
3. Проверьте логи Make.com

### Проблема: "Дубликаты публикаций"

**Решение:** Система автоматически проверяет existing_posts перед публикацией. Если задача уже опубликована, повторная публикация не произойдет.

---

## API Endpoints

### Callback от Make.com (опционально)

**POST** `/api/webhooks/social-media-callback/`

Request body:
```json
{
  "task_id": 123,
  "platform": "instagram",
  "status": "published",
  "post_url": "https://instagram.com/p/...",
  "post_id": "123456789"
}
```

Response:
```json
{
  "status": "ok",
  "message": "Status updated for task 123 on instagram"
}
```

---

## Безопасность

- Access tokens хранятся в БД (рекомендуется шифрование на уровне БД)
- Webhook endpoints открыты (для Make.com)
- Рекомендуется добавить проверку подписи в SocialMediaCallbackView

---

## Дополнительные возможности

### Retry логика

В будущем можно добавить Celery задачу для автоматического повтора неудачных публикаций:

```python
@shared_task
def retry_failed_social_posts():
    """Повторяет неудачные публикации."""
    failed_posts = SocialMediaPost.objects.filter(
        status='failed',
        retry_count__lt=3
    )
    
    for post in failed_posts:
        # Retry logic...
```

### Аналитика

Создайте представление для аналитики публикаций:

```python
# Статистика по платформам
SocialMediaPost.objects.values('platform').annotate(
    total=Count('id'),
    published=Count('id', filter=Q(status='published'))
)
```

---

## Требования

### Python пакеты

Убедитесь, что установлены:
- `requests` - для API запросов (уже должен быть)
- Django 4.x+ - для JSONField

### Docker

Пересоберите контейнер после изменений:

```bash
docker compose build quiz_backend
docker compose restart quiz_backend
```

### Миграции

Примените миграции:

```bash
docker compose exec quiz_backend python manage.py migrate tasks
docker compose exec quiz_backend python manage.py migrate webhooks
```

---

## Тестирование

### 1. Тест API (Pinterest)

1. Настройте credentials для Pinterest
2. Создайте задачу с изображением
3. Установите `published=True`
4. Проверьте логи: должна появиться запись "🌐 Автопубликация задачи X в соцсети"
5. Проверьте Pinterest - пин должен появиться на доске
6. В админке → Social Media Posts - проверьте статус "Опубликовано"

### 2. Тест Webhook (Instagram через Make.com)

1. Настройте сценарий в Make.com
2. Добавьте webhook в Django админке
3. Создайте задачу и опубликуйте
4. Проверьте логи Make.com - данные должны прийти
5. Проверьте Instagram - пост должен появиться

### 3. Ручная публикация

1. Создайте несколько задач
2. Выберите их в списке
3. Actions → "📱 Опубликовать в соцсети"
4. Проверьте результаты в сообщениях админки

---

## Часто задаваемые вопросы

**Q: Можно ли отключить автопубликацию?**
A: Да, закомментируйте сигнал `auto_publish_to_social_media` в `tasks/signals.py`

**Q: Как добавить новую платформу?**
A: 
1. Для API: создайте `platforms/new_platform_api.py`
2. Добавьте в `API_PLATFORMS` или `WEBHOOK_PLATFORMS`
3. Добавьте обработку в `_publish_via_api()` или `_publish_via_webhook()`

**Q: Можно ли публиковать только в определенные платформы?**
A: Да, деактивируйте ненужные credentials (is_active=False) или webhook

**Q: Что делать если публикация провалилась?**
A: Проверьте `error_message` в Social Media Posts и retry_count. При необходимости используйте action "Опубликовать в соцсети" для повторной попытки.

---

## Поддержка

При возникновении проблем:
1. Проверьте логи: `quiz_backend_logs/debug.log`
2. Проверьте статусы в Social Media Posts
3. Проверьте credentials и webhooks в админке

