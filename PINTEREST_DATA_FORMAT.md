# Формат данных для Pinterest

## 📤 Что передается в Pinterest API

### Структура запроса (JSON)

```json
{
  "board_id": "mr_fulani/test",
  "title": "Что делает это приложение FastAPI?",
  "description": "Что делает это приложение FastAPI?\n\n• Создает REST API\n• Обрабатывает HTTP запросы\n• Управляет базой данных\n• Генерирует документацию\n\n💡 Правильный ответ: Создает REST API\n\n#programming #coding #quiz",
  "link": "https://quiz-code.com/task/57",
  "media_source": {
    "source_type": "image_url",
    "url": "https://s3.amazonaws.com/bucket/image.jpg"
  }
}
```

### Параметры

| Параметр | Источник | Ограничения | Пример |
|----------|----------|-------------|--------|
| `board_id` | `SocialMediaCredentials.extra_data['board_id']` | Формат: `user_id/board_name` или числовой ID | `"mr_fulani/test"` |
| `title` | `TaskTranslation.question` | До 100 символов | `"Что делает это приложение FastAPI?"` |
| `description` | Форматированное описание (см. ниже) | До 500 символов | См. пример выше |
| `link` | `Task.external_link` или `SITE_URL/task/{id}` | URL задачи | `"https://quiz-code.com/task/57"` |
| `image_url` | `Task.image_url` | Публично доступный URL | `"https://s3.amazonaws.com/..."` |

---

## 📝 Формат описания (description)

### Структура

```
{Вопрос}

• Вариант ответа 1
• Вариант ответа 2
• Вариант ответа 3
• Вариант ответа 4

💡 Правильный ответ: {correct_answer}

#programming #coding #quiz
```

### Пример реального описания

```
Что делает это приложение FastAPI?

• Создает REST API
• Обрабатывает HTTP запросы
• Управляет базой данных
• Генерирует документацию

💡 Правильный ответ: Создает REST API

#programming #coding #quiz
```

### Логика формирования

1. **Вопрос** — берется из `TaskTranslation.question` (до 100 символов для title, без ограничений в description)
2. **Варианты ответов** — первые 4 варианта из `TaskTranslation.answers`, каждый с маркером `•`
3. **Правильный ответ** — из `TaskTranslation.correct_answer` с эмодзи 💡
4. **Хештеги** — автоматически добавляются: `#programming #coding #quiz`

---

## 🎨 Как это будет выглядеть в Pinterest

### Визуальное представление

```
┌─────────────────────────────────────┐
│                                     │
│   [ИЗОБРАЖЕНИЕ ЗАДАЧИ]              │
│   (из Task.image_url)               │
│                                     │
├─────────────────────────────────────┤
│ 📌 Что делает это приложение        │
│    FastAPI?                         │
│                                     │
│ • Создает REST API                  │
│ • Обрабатывает HTTP запросы         │
│ • Управляет базой данных            │
│ • Генерирует документацию           │
│                                     │
│ 💡 Правильный ответ: Создает        │
│    REST API                         │
│                                     │
│ #programming #coding #quiz          │
│                                     │
│ 🔗 quiz-code.com/task/57            │
└─────────────────────────────────────┘
```

### Элементы пина

1. **Изображение** — главная картинка задачи (сверху)
2. **Заголовок** — вопрос задачи (до 100 символов)
3. **Описание** — полный текст с вариантами ответов и правильным ответом
4. **Ссылка** — кликабельная ссылка на задачу на вашем сайте
5. **Хештеги** — для поиска и категоризации

---

## 📊 Пример полного запроса

### Исходные данные из БД

```python
task = Task(
    id=57,
    image_url="https://s3.amazonaws.com/bucket/task_57.jpg",
    external_link="https://quiz-code.com/task/57"
)

translation = TaskTranslation(
    question="Что делает это приложение FastAPI?",
    answers=["Создает REST API", "Обрабатывает HTTP запросы", "Управляет базой данных", "Генерирует документацию"],
    correct_answer="Создает REST API"
)

creds = SocialMediaCredentials(
    platform="pinterest",
    extra_data={"board_id": "mr_fulani/test"}
)
```

### Что отправляется в API

```python
POST https://api.pinterest.com/v5/pins
Headers:
  Authorization: Bearer {access_token}
  Content-Type: application/json

Body:
{
  "board_id": "mr_fulani/test",
  "title": "Что делает это приложение FastAPI?",
  "description": "Что делает это приложение FastAPI?\n\n• Создает REST API\n• Обрабатывает HTTP запросы\n• Управляет базой данных\n• Генерирует документацию\n\n💡 Правильный ответ: Создает REST API\n\n#programming #coding #quiz",
  "link": "https://quiz-code.com/task/57",
  "media_source": {
    "source_type": "image_url",
    "url": "https://s3.amazonaws.com/bucket/task_57.jpg"
  }
}
```

### Ответ API (успех)

```json
{
  "id": "1234567890123456789",
  "board_id": "mr_fulani/test",
  "title": "Что делает это приложение FastAPI?",
  "description": "...",
  "link": "https://quiz-code.com/task/57",
  "media": {
    "images": {
      "564x": {
        "url": "https://i.pinimg.com/564x/...",
        "width": 564,
        "height": 564
      }
    }
  },
  "created_at": "2025-11-30T17:25:00Z"
}
```

---

## 🔗 Ссылка на пин

После успешного создания пина формируется URL:

```
https://pinterest.com/pin/{pin_id}
```

Например:
```
https://pinterest.com/pin/1234567890123456789
```

---

## ⚠️ Ограничения

1. **Title** — максимум 100 символов (обрезается автоматически)
2. **Description** — максимум 500 символов (обрезается автоматически)
3. **Изображение** — должно быть публично доступно по URL
4. **Board ID** — должен существовать и быть доступен для записи

---

## 📝 Код формирования

```python
# quiz_backend/tasks/services/social_media_service.py

def _format_pinterest_description(translation: TaskTranslation) -> str:
    """Форматирует описание для Pinterest."""
    # Парсим ответы
    answers = translation.answers if isinstance(translation.answers, list) else json.loads(translation.answers)
    
    # Форматируем варианты ответов
    answer_lines = [f"• {ans}" for ans in answers[:4]]
    answer_text = "\n".join(answer_lines)
    
    description = f"{translation.question}\n\n{answer_text}\n\n💡 Правильный ответ: {translation.correct_answer}"
    
    # Добавляем хештеги
    description += "\n\n#programming #coding #quiz"
    
    return description[:500]  # Обрезаем до лимита Pinterest
```

---

## 🎯 Итог

**В Pinterest будет создан пин с:**
- ✅ Изображением задачи
- ✅ Вопросом в качестве заголовка
- ✅ Вариантами ответов в описании
- ✅ Правильным ответом с эмодзи 💡
- ✅ Хештегами для поиска
- ✅ Ссылкой на задачу на вашем сайте

**Пользователь увидит:**
- Красивую карточку с вопросом и вариантами
- Может кликнуть на ссылку, чтобы перейти к задаче
- Может сохранить пин на свою доску
- Может поделиться пином

