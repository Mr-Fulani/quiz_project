<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
{% spaceless %}
{% for url in urlset %}
    <url>
        <loc>{{ url.location|escape }}</loc>
        {% if url.lastmod %}<lastmod>{{ url.lastmod|date:"Y-m-d" }}</lastmod>{% endif %}
        {% if url.changefreq %}<changefreq>{{ url.changefreq }}</changefreq>{% endif %}
        {% if url.priority %}<priority>{{ url.priority }}</priority>{% endif %}
        {% if url.images %}
            {% for image in url.images %}
                <image:image>
                    <image:loc>{{ image.loc|escape }}</image:loc>
                    {% if image.title %}<image:title>{{ image.title|escape }}</image:title>{% endif %}
                    {% if image.caption %}<image:caption>{{ image.caption|escape }}</image:caption>{% endif %}
                </image:image>
            {% endfor %}
        {% endif %}
        {% if url.alternates %}
            {% for alternate in url.alternates %}
                <xhtml:link rel="alternate" hreflang="{{ alternate.lang }}" href="{{ alternate.location|escape }}" />
            {% endfor %}
        {% endif %}
    </url>
{% endfor %}
{% endspaceless %}
</urlset>

