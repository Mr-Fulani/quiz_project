/**
 * Обновление счетчиков прогресса при возвращении на страницу
 * Обрабатывает событие pageshow для обновления данных при возврате из кэша браузера
 */
(function() {
    'use strict';

    // Проверяем, что мы на странице с прогрессом
    const isProgressPage = document.querySelector('.progress-info, .progress-bar-container');
    if (!isProgressPage) {
        return; // Не на странице с прогрессом, выходим
    }

    /**
     * Обработчик события pageshow
     * Срабатывает при показе страницы, включая возврат из кэша браузера
     */
    window.addEventListener('pageshow', function(event) {
        // event.persisted = true означает, что страница загружена из кэша браузера
        if (event.persisted) {
            console.log('📦 Страница загружена из кэша браузера, обновляем прогресс...');
            // Небольшая задержка для того, чтобы страница полностью загрузилась
            // Затем перезагружаем страницу для получения актуальных данных
            setTimeout(function() {
                window.location.reload();
            }, 100);
        }
    });

    console.log('✅ Скрипт обновления прогресса инициализирован');
})();
