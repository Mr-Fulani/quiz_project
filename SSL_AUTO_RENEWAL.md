# 🔐 Автоматическое обновление SSL сертификатов

## 📋 Обзор

Let's Encrypt сертификаты действительны 90 дней. Для предотвращения истечения сертификатов настроено автоматическое обновление через cron.

## 🚀 Быстрая настройка

На удаленном сервере выполните:

```bash
cd /opt/quiz_project/quiz_project
./setup-ssl-auto-renewal.sh
```

Это настроит:
- ✅ Cron задачу для ежедневной проверки сертификатов (в 03:00)
- ✅ Автоматическую перезагрузку nginx после обновления
- ✅ Логирование в `/var/log/ssl-renewal.log`

## 📝 Что происходит автоматически

1. **Ежедневно в 03:00** cron запускает проверку сертификатов
2. Certbot проверяет, какие сертификаты скоро истекут (в течение 30 дней)
3. Если сертификаты требуют обновления - они обновляются автоматически
4. Nginx перезагружается для применения новых сертификатов
5. Все действия логируются в `/var/log/ssl-renewal.log`

## 🔍 Проверка статуса

### Просмотр cron задач
```bash
crontab -l | grep renew-ssl
```

### Просмотр логов
```bash
# Последние записи
tail -20 /var/log/ssl-renewal.log

# Мониторинг в реальном времени
tail -f /var/log/ssl-renewal.log
```

### Ручной запуск обновления
```bash
# Автоматический скрипт (для cron)
./renew-ssl-auto.sh

# Или интерактивный скрипт (с dry-run)
./renew-ssl.sh
```

## 🛠️ Устранение проблем

### Cron задача не выполняется

1. Проверьте, что cron запущен:
   ```bash
   systemctl status cron
   # или
   systemctl status crond
   ```

2. Проверьте права на скрипт:
   ```bash
   chmod +x renew-ssl-auto.sh
   ```

3. Проверьте логи cron:
   ```bash
   grep CRON /var/log/syslog
   # или
   journalctl -u cron
   ```

### Сертификаты не обновляются

1. Проверьте логи:
   ```bash
   tail -50 /var/log/ssl-renewal.log
   ```

2. Проверьте доступность доменов:
   ```bash
   curl -I http://quiz-code.com/.well-known/acme-challenge/test
   ```

3. Проверьте, что nginx запущен:
   ```bash
   docker compose -f docker-compose.local-prod.yml ps nginx
   ```

### Nginx не перезагружается

Если автоматическая перезагрузка не работает, nginx можно перезапустить вручную:
```bash
docker compose -f docker-compose.local-prod.yml restart nginx
```

## 🔄 Удаление автоматического обновления

Если нужно отключить автоматическое обновление:

```bash
# Удалить cron задачу
crontab -l | grep -v "renew-ssl-auto.sh" | crontab -

# Проверить, что задача удалена
crontab -l | grep renew-ssl
```

## 📚 Дополнительная информация

- **Срок действия сертификатов**: 90 дней
- **Автоматическое обновление**: за 30 дней до истечения
- **Частота проверки**: ежедневно в 03:00
- **Логи**: `/var/log/ssl-renewal.log`

## 💡 Рекомендации

1. **Регулярно проверяйте логи** - хотя бы раз в месяц
2. **Мониторьте срок действия** - проверяйте сертификаты перед важными событиями
3. **Настройте уведомления** - можно добавить отправку email при обновлении сертификатов

